#!/bin/sh

set -e

GRUBFILE="/etc/default/grub"

. /usr/share/debconf/confmodule
db_version 2.0

if [ -f $GRUBFILE ]; then
  db_set scramlkb/grub true
fi

db_input high scramlkb/grub || true
db_go

exit 0
